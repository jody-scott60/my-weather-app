export type CoordinatesArrEntry = {
  name: string;
  country_code: string;
  latitude: string;
  longitude: string;
};

export type CoordinatesResult = {
  name: string;
  latitude: string;
  longitude: string;
};
